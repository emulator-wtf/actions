name: Update dist branch

on:
  push:
    branches:
    - main
  workflow_dispatch:

concurrency:
  group: update-dist
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  update-dist:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout main
      # zizmor: ignore[artipacked]
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        path: source

    - name: Setup Node
      uses: actions/setup-node@v6.3.0 # zizmor: ignore[unpinned-uses] immutable tag
      with:
        node-version: '24'

    - name: Setup mise
      uses: jdx/mise-action@v4.0.1 # zizmor: ignore[unpinned-uses] immutable tag
      with:
        install: false

    - name: Build dist
      working-directory: source
      run: mise dist


    - name: Checkout dist branch
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        ref: dist
        path: dist-branch
        persist-credentials: false

    - name: Sync files to dist branch
      run: |
        cd dist-branch
        # Remove everything except .git
        find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +

        # Copy specified directories
        cp -a ../source/dist .
        cp -a ../source/configure-credentials .
        cp -a ../source/invoke .
        cp -a ../source/invoke-session .
        cp -a ../source/run-tests .
        cp -a ../source/setup-ew-cli .
        cp -a ../source/use-emulator .

        # Copy specified files
        cp ../source/LICENSE .
        cp ../source/package.json .

    - name: Commit and push
      working-directory: dist-branch
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

        MAIN_SHA="${{ github.sha }}"

        git add -A

        # Check if there are changes to commit
        if git diff --cached --quiet; then
          echo "No changes to commit"
          exit 0
        fi

        git commit -m "ci: build latest dist from ($MAIN_SHA)"
        git push origin HEAD:dist
